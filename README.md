https://github.com/AlexNovokrest/weblarek
# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные
Описывает интерфейсы данных, которые были использованы в проекте:

#### Интейфейс IProduct
interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}
Описывает данные товара, представленного в каталоге. Состоит из названия, описания, изображения, категории и стоимости.
#### Интейфейс IBuyer
interface IBuyer {
  payment: TPayment;
  email: string;
  phone: string;
  address: string;
}
Описывает данные покупателя, необходимые для заказа. Состоит из способа оплаты, адреса, адреса электронной почты и номера телефона.

#### Тип оплаты TPayment
TPayment = "cash" | "card" | null
Описивает выбранный пользователем способ оплаты.
Может быть пустым, если тип оплаты еще не выбран.
Возможные способы оплаты:
cash - наличными при получении
card - банковской картой
null - способ оплаты еще не выбран

#### Интерфейс IOrder
Наследует свойства интерфейса IBuyer
Описывает структуру данных заказа для отправки на сервер.
interface IOrder extends IBuyer{
  items: string[]; - массив id товаров в заказе
  total: number; - общая стоимость заказа
}

#### Интерфейс IProductResponse
interface IProductResponse {
  items: IProduct[];
  total: number;
}
Описывает структуру ответа сервера при запросе списка товаров
items: IProduct[]; - массив товаров
total: number; - общее колличество товаров

#### Интерфейс IOrderResult
interface IOrderResult {
  id: string;
  total: number;
}
Результат создания заказа
Описывает структуру ответа сервера при создании заказа
id: string - уникальный идентификатор созданного заказа
total: number - общая стоимость заказа


#### Модель данных 

##### Модель Корзина (Basket) - класс корзины покупок
Содержит методы по управлению товарами в корзине 

constructor() - создлаёт пустую корзину
private items: IProduct[] - массив товаров, которые уже добавлены в корзину

Методы:
getItems() - возвращает массив всех товаров в корзине
addItem() - добавляет товар в корзину
deleteItem() - удаляет товар из корзины
clear() - очищает корзину
getTotalPrice() - общая стоимость товаров, добавленных в корзину
getCount() - колличесвтов товаров в корзине
hasItem() - наличие товара в корзине по его id(true or false)

##### Модель Покупатель (Buyer) - класс данных покупателя
Содержит методы для управления информацией о покупателе и её валидации

constructor() - создает пустые данные покупателя
Методы:
setData() - сохраняет данные покупателя
getData() - возвращает данные покупателя
clear() - очищает данные покупателя
validate() - валидация данных покупателя

##### Модель Каталог (Catalog) - класс каталога товаров
Содержит методы управления списком товаров

constructor() - создаёт пустой каталог

Поля класса:
private items: IProduct[] - массив товаров в каталоге
private selectedItem: IProduct | null - текущий выбранный товар

Методы:
setItems() - устанавливает список товаров в каталоге
getItems() - возвращиет все товары из каталога
setSelectedItem() - устанавливает товар как выбранный
getSelectedItems - возвращает выбранный товар или null, если ничего не выбрано
getItemById - возвращает товар по id 

##### Слой коммуникации (ApiService)
Класс ApiService - сервис для взаимодействия с API для обмена данных с сервером

Методы:
getProductList() - получение списка товаров с сервера
createOrder() - отправка заказа на сервер и возвращает результат

## Слой представления (View)

Слой View отвечает за отображение. Находит элементы разметки, обновляет их.

### Класс Header
Класс Header отвечает за отображение шапки приложения и счетчика товаров в корзине.

Поля класса:
counterElement: HTMLElement - счетчик товаров в корзине
basketButton: HTMLButtonElement - кнопка открытия корзины

Методы:
set counter(value: number) - обновляет значение счетчика корзины.

basket:open - событие при клике на кнопку корзины.

### Класс Modal (Модальное окно)
Управление отображением модального окна и его содержимым.

Поля класса
closeButton: HTMLButtonElement - кнопка закрытия моодального окна
contentElement: HTMLElement - коетейнер для содержимого модального окна
events: IEvents - брокер событий

Методы:
set content — устанавливает содержимое модального окна.
open() — открывает модальное окно, добавляя CSS класс modal_active. Генерирует событие modal:open.  
close() - закрывает модальное окно, удаляя CSS класс modal_active и очищая содержимое. Генерирует событие modal:close.  
render() — принимает данные, вызывает родительский render, открывает модальное окно и возвращает контейнер.

### Класс Gallery
Отображение каталога товаров на главной странице.

Методы:
set catalog() - принимает массив элементов карточек товаров и отображает их.

### Класс Basket
Отоюражение списка товаров в корзине и общую стоимость.

Поля класса:
listElement: HTMLElement - контейнер для списка товаров.
totalElement: HTMLElement - общая стоимость.
buttonElement: HTMLButtonElement - кнопка оформления заказа.
events: IEvents - брокер событий.

Методы:
set items() - устанавливает список товаров в корзине.
set tota() - устанавливает общую стоимость.

basket:order - событие при клике на кнопку оформления.

### Карточки товара
Карточки товара представлены в виде:
Card - базовый класс.
CardBasket - карточка в корзине.
CardCatalog - карточка в каталоге.
CardPreview - карточка в модальном окне.

###  Класс Card

Поля класса:  
titleElement: HTMLElement —  заголовок карточки.
priceElement: HTMLElement — цена.

Методы:  
set id() — устанавливает идентификатор товара.  
get id() — возвращает идентификатор товара.  
set title() — устанавливает название товара.  
get title() — возвращает название товара.  
set price() — цена с добавлением текста "синапсов". Если цена null, отображается текст "Бесценно".

### Класс CardBasket

Поля класса:  
indexElement: HTMLElement — индекс товара.  
deleteButton: HTMLButtonElement — кнопка удаления товара из корзины.

Методы:  
set index() — устанавливает порядковый номер товара в корзине.

### Класс CardCatalog

Поля класса:  
imageElement: HTMLImageElement — изображение товара.  
categoryElement: HTMLElement — категория товара.

Методы:  
set category() — устанавливает категорию товара.
set image() — устанавливает изображение товара.

### Класс CardPreview

Поля класса:  
imageElement: HTMLImageElement — изображение товара.  
categoryElement: HTMLElement — категория товара.  
descriptionElement: HTMLElement —описание товара.  
buttonElement: HTMLButtonElement — кнопка действия.

Методы:  
set category() — устанавливает категорию товара.
set image() — устанавливает изображение товара.
set description() — устанавливает описание товара.  
set buttonText() — устанавливает текст кнопки.  
set buttonDisabled() — устанавливает состояние кнопки.

### Классы форм
Формы представленны в виде:
Form — базовый класс формы
FormOrder — шаг 1 (оплата + адрес)
FormContacts — шаг 2 (почта + телефон)

### Класс Form

Поля класса:  
submitButton: HTMLButtonElement — кнопка отправки формы.  
errorsElement: HTMLElement — отображениt ошибок валидации.  
events: IEvents — брокер событий.

Методы:  
set valid() — устанавливает валидность формы. При значении true кнопка отправки активна, при false — неактивна.  
set errors() — устанавливает текст ошибок валидации.  
render() — отображает форму с данными и состоянием валидации.

### Класс FormOrder

Поля класса:  
onlineButton: HTMLButtonElement — выбор оплаты картой.  
cashButton: HTMLButtonElement — оплата наличными.  
addressInput: HTMLInputElement — адреса доставки.

Методы:  
set payment() — устанавливает выбранный способ оплаты. Добавляет класс `button_alt-active` к соответствующей кнопке и убирает у другой.  
set address() — устанавливает значение в поле ввода адреса доставки.

order:payment - событие при клике на кнопку "картой" или "наличными"

### Класс FormContacts

Поля класса:  
emailInput: HTMLInputElement — ввод email.  
phoneInput: HTMLInputElement — ввода телефона.

Методы:  
set email() — устанавливает значение в поле ввода email.  
set phone() — устанавливает значение в поле ввода телефона.

### Класс Success
Экран успешного оформления заказа.

Поля класса:  
descriptionElement — описание и отображене итоговой суммы заказа.  
closeButton — кнопка закрытия.

Методы:  
set total() — устанавливает итоговую сумму заказа.

## Презентер
Презентер содержит основную логику приложения и отвечает за связь между слоями данных и представления. В проекте презентер реализован в файле main.ts в виде обработчиков событий.

Презентер подписывается на события от моделей данных и компонентов представления, обрабатывает их и вызывает соответствующие методы моделей или представлений для обновления состояния приложения.


### События

От моделей данных:
catalog:changed — изменение списка товаров в каталоге.
preview:changed — изменение выбранного товара для просмотра.
cart:changed — изменение состава корзины.
customer:changed — изменение данных покупателя.
card:select — выбор карточки товара в каталоге.

От компонентов представления:
modal:open — открытие модального окна.
modal:close — закрытие модального окна.
basket:open - пользователь открыл корзину.
basket:start - начало оформления заказа.
order:payment — выбор способа оплаты.
order.address:change — изменение адреса доставки.
order:submit — отправка формы оплаты и адреса.
contacts.email:change — изменение email.
contacts.phone:change — изменение телефона.
contacts:submit — отправка формы контактов и завершение заказа.